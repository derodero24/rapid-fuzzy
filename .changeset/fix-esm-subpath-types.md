---
"rapid-fuzzy": patch
---

Fix TypeScript `node16`/`nodenext` ESM resolution for `rapid-fuzzy/highlight` and `rapid-fuzzy/objects` subpath exports by adding proper `.d.mts` type declarations and fixing import specifiers in `index.d.mts`.
